services:
  folio:
    build: .
    ports:
      - "8080:8080"
    environment:
      - WebDav__Url=${WEBDAV_URL}
      - WebDav__Username=${WEBDAV_USERNAME}
      - WebDav__Password=${WEBDAV_PASSWORD}
    restart: unless-stopped
